{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "chrome",
        "request": "launch",
        "name": "Launch Chrome against localhost",
        "url": "http://localhost:8000", // Open in browser at this URL
        "webRoot": "${workspaceFolder}" // Set the root of your project
      }
    ]
  }